Через отдельный файл. В этом случае файлы с расширением .html хранят html-код, а файлы с расширением .css — код на CSS.
Когда стили выносят в отдельный CSS-файл, html-страница связывается с ним тегом <link>:
